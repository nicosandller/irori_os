[package]
name = "irori-rules"
description = "Deterministic rules engine: pure, with injected Clock and StateView."
version.workspace = true
edition.workspace = true
publish.workspace = true
repository.workspace = true

[dependencies]
irori-types.workspace = true
# Successor of `cel-interpreter` (ROADMAP D41). No chrono/regex: the surface does not use them.
cel = { version = "0.14.5", default-features = false }

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
